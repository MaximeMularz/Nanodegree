<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hostabee.nanodegree" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:allowBackup="true"
            android:name=".activity.SearchForAnArtistActivity"
            android:label="@string/title_activity_spotify_streamer"
            android:launchMode="singleTop">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        </activity>
        <activity
            android:name=".activity.TopTenTracksActivity"
            android:label="@string/title_activity_top_ten_tracks"
            android:parentActivityName=".activity.SearchForAnArtistActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.SearchForAnArtistActivity" />
        </activity>
        <activity
            android:name=".activity.TrackPlayerActivity"
            android:label="@string/title_activity_track_player"
            android:parentActivityName=".activity.TopTenTracksActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.hostabee.nanodegree.activity.TopTenTracksActivity" />
        </activity>
    </application>

</manifest>
